# Copy this file to .env and replace the URI with your MongoDB connection string.
# Never commit .env or expose MONGODB_URI to the React frontend.

MONGODB_URI=YOUR_MONGODB_CONNECTION_STRING
PORT=5000

# Optional: required as the x-admin-key header to call DELETE /api/resources/:id
# ADMIN_KEY=
